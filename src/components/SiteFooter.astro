---
import { siteConfig } from '../config/site';
import BrandEmblem from './BrandEmblem.astro';
import { routes, withBase } from '../lib/url';

// Emblem maksimal satu kali per halaman: beranda sudah menampilkannya sebagai objek pameran di bagian filosofi (HomePhilosophy).
const showEmblem = Astro.url.pathname !== withBase(routes.home);
---

<footer class="site-footer">
  {showEmblem && <BrandEmblem variant="footer" />}
  <div class="site-footer__text">
    <p class="site-footer__attribution">{siteConfig.attribution}</p>
    <p class="site-footer__meta">
      {siteConfig.product} adalah bagian dari {siteConfig.masterBrand}. Konten bersifat edukasi, bukan saran
      investasi atau sinyal trading.
    </p>
  </div>
</footer>

<style>
  .site-footer {
    display: flex;
    align-items: center;
    gap: var(--space-6);
    margin-top: var(--space-16);
    padding: var(--space-8) 0 var(--space-12);
    border-top: var(--border-hair) solid var(--rule);
    font-size: var(--text-sm);
    color: var(--ink-3);
  }

  .site-footer p {
    margin: 0;
    max-width: var(--measure);
  }

  .site-footer__attribution {
    font-weight: 600;
    color: var(--ink-2);
  }

  .site-footer .site-footer__meta {
    margin-top: var(--space-1);
  }
</style>
